services:
  - type: web
    name: ghl-webhook-processor
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GHL_WEBHOOK_URL
        sync: false
